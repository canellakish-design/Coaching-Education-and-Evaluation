import React from "react";
import ReactDOM from "react-dom/client";
import CoachingPlatform from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CoachingPlatform />
  </React.StrictMode>
);
